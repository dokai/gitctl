[buildout]
newest = false
develop = .
parts =
    python
    test

[python]
recipe = zc.recipe.egg
eggs =
    gitctl
    setuptools-git
    nose
interpreter = python

[test]
recipe = zc.recipe.testrunner
eggs = gitctl
defaults = ['--auto-color', '-v']
